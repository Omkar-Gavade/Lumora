node_modules
dist
*.tsbuildinfo

.env
.env.local

# Local file storage for uploaded documents (M3). Never committed.
uploads

# Test env (gitignored)
.env.test
coverage

# Backups of the secrets file are secrets too.
.env.bak*
