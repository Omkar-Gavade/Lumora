# Frontend environment. Copy to `.env`.
#
# Only `VITE_`-prefixed variables reach the browser — Vite refuses to expose
# anything else, which is what stops a server secret being inlined into a
# bundle by accident.

# Absolute origin of the API. No trailing slash.
VITE_API_URL=http://localhost:4000
